# Roadmap

Candidate and in-flight feature work for Manashelper. This is a planning doc, not a changelog — see `git log` for
what's actually shipped.

When adding an entry, link the domain/guide it touches so the next person has a starting point
(e.g. `docs/guides/adding-a-telegram-handler.md`, `docs/ARCHITECTURE.md#request-flow-obis-student-portal-access`).

## Planned

_Nothing scheduled yet — add entries here as they're committed to._

## Under consideration

_Ideas not yet committed to. Move to Planned once someone owns it._

## Done

_Move completed items here with the date/PR, or delete once they're old enough that `git log` is the better
reference._
